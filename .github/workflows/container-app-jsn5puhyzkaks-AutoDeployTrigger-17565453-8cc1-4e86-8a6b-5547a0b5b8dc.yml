name: Trigger auto deployment for container-app-jsn5puhyzkaks

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-jsn5puhyzkaks-AutoDeployTrigger-17565453-8cc1-4e86-8a6b-5547a0b5b8dc.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPJSN5PUHYZKAKS_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPJSN5PUHYZKAKS_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPJSN5PUHYZKAKS_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPJSN5PUHYZKAKS_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPJSN5PUHYZKAKS_REGISTRY_PASSWORD }}
          containerAppName: container-app-jsn5puhyzkaks
          resourceGroup: expresssample8d83a53a
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


